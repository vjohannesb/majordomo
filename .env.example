# Majordomo MCP Server Configuration

# Database (PostgreSQL)
DATABASE_URL=postgres://user:pass@host:5432/majordomo

# Google OAuth (for Gmail + Calendar)
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# Slack OAuth (optional - for Slack integration)
# Create app at https://api.slack.com/apps
SLACK_CLIENT_ID=your-slack-client-id
SLACK_CLIENT_SECRET=your-slack-client-secret

# Linear OAuth (optional - for Linear integration)
# Create app at https://linear.app/settings/api/applications/new
LINEAR_CLIENT_ID=your-linear-client-id
LINEAR_CLIENT_SECRET=your-linear-client-secret
# Linear Webhook secret (from webhook settings page)
LINEAR_WEBHOOK_SECRET=your-linear-webhook-signing-secret

# Notion OAuth (optional - for Notion integration)
# Create integration at https://www.notion.so/my-integrations
# Enable "Public Integration" and set OAuth redirect to {BASE_URL}/auth/notion/callback
NOTION_CLIENT_ID=your-notion-client-id
NOTION_CLIENT_SECRET=your-notion-client-secret

# Server
PORT=3000
BASE_URL=https://your-app.railway.app
NODE_ENV=production
